# @package _global_
# Experiment configuration for position embedding ablation study (RoPE vs NoPE)
defaults:
  - override /model: small_no_rope  # Use no-rope model by default

# Reduced token count for ablation study
total_tokens: 81920000  # ~1/5 of full training for faster ablation

wandb_name: "ablation-position-emb-rope${model.use_rope}"
wandb_project: "cs336-ablations"
