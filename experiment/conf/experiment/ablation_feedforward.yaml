# @package _global_
# Experiment configuration for SwiGLU vs SiLU ablation study
defaults:
  - override /model: small_silu  # Use SiLU model by default

# Reduced token count for ablation study
total_tokens: 81920000  # ~1/5 of full training for faster ablation

wandb_name: "ablation-ffn-${model.ffn_type}"
wandb_project: "cs336-ablations"
